{
  "name": "orqent",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.7.0",
  "scripts": {
    "build": "pnpm -r --sort run build",
    "check": "pnpm -r --sort run check"
  },
  "devDependencies": {
    "typescript": "^6.0.3"
  }
}
