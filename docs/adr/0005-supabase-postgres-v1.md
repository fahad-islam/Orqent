# Supabase Postgres for v1

Orqent will use Supabase and Postgres for v1 durable state. Supabase Auth can provide authentication and sessions, while Orqent keeps workspace membership, roles, approval policy, provider mappings, workflow state, audit history, and business authorization in its own domain model.
