# Human approval for v1 External Writes

All v1 External Writes require human approval before execution. Orqent may plan, dry-run, summarize, and prepare Proposed Actions automatically, but creating or updating provider records, sending customer-facing messages, sending Contracts, and changing CRM state must pass through an Approval Request and immutable approved payload.
