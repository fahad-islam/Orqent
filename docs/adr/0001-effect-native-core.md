# Effect-native core

Orqent's backend and worker core will be Effect-native. Workflow execution, provider connectors, policies, schemas, typed errors, retries, configuration, and tests should be built with Effect services, layers, Schema, and typed error channels so external side effects stay composable, observable, and testable.
